#!/bin/bash
# .Blood_Splatter

if ! echo "$inventory" | grep .Blood_Splatter > /dev/null; then

cat << EOF
There seems to be a splatter of blood on Mary's dress.

Put a 'picture' of this in your inventory by typing "take .Blood_Splatter"
(This creates a variable and adds on a file location to the variable, creating a list of file locations)

You can view your inventory by typing "\$inventory",
EOF

else 
cat << EOF
There seems to be a splatter of blood on Mary's dress.

EOF

fi
